// Unlisted case study: reachable by direct link, kept out of search results.
export const metadata = {
  title: 'Easing out ₹20,000 crore of payments for 40+ enterprises | KredX TReDS — Adit Saxena',
  description:
    'A parallel workflow that replaced a blind bulk-upload-and-pray process with a validated, real-time entry flow — after an RBI mandate pushed upload volume up 400% and exposed a ~83% failure rate.',
  robots: { index: false, follow: false, nocache: true },
};

export default function KredXExpressLayout({ children }) {
  return children;
}
