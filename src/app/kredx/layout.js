// Unlisted case study: reachable by direct link, kept out of search results.
export const metadata = {
  title: 'Redesigning the Factoring Unit Flow | KredX TReDS — Adit Saxena',
  description:
    'Replacing a CSV + ZIP upload workflow with direct PDF upload and IDP auto-extraction — cutting factoring unit creation time by 56%.',
  robots: { index: false, follow: false, nocache: true },
};

export default function KredXLayout({ children }) {
  return children;
}
